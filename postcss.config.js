/**
 * ============================================================
 * postcss.config.js
 * ============================================================
 * PostCSS plugins used by Vite during CSS processing:
 *   - tailwindcss   → generates utility classes from config
 *   - autoprefixer  → adds vendor prefixes for cross-browser support
 * ============================================================
 */

export default {
  plugins: {
    tailwindcss:  {},
    autoprefixer: {}
  }
};
